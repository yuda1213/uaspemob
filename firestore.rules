rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode - Allow read/write untuk testing
    // JANGAN GUNAKAN DI PRODUCTION!
    match /{document=**} {
      allow read, write: if true;
    }

    // Production rules (uncomment untuk production)
    /*
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
      
      match /favorites/{document=**} {
        allow read, write: if request.auth.uid == userId;
      }
    }

    // Laundries collection
    match /laundries/{laundryId} {
      allow read: if true;
      allow write: if request.auth.uid == resource.data.ownerId;
      
      match /services/{document=**} {
        allow read: if true;
        allow write: if request.auth.uid == get(/databases/$(database)/documents/laundries/$(laundryId)).data.ownerId;
      }
    }

    // Orders collection
    match /orders/{orderId} {
      allow read, write: if request.auth.uid == resource.data.userId || request.auth.uid == resource.data.laundryId;
    }

    // Offers collection
    match /offers/{offerId} {
      allow read: if true;
      allow write: if request.auth.uid == resource.data.laundryId;
    }

    // Reviews collection
    match /reviews/{reviewId} {
      allow read: if true;
      allow write: if request.auth.uid == resource.data.userId;
    }
    */
  }
}
